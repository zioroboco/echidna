{
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @echidna/speak start",
    "docker": "docker-compose -f .docker/docker-compose.yml run --rm",
    "docker:bash": "yarn -s docker echidna /bin/bash",
    "bash": "yarn -s docker:bash"
  },
  "devDependencies": {
    "@types/node": "^12.0.2",
    "lerna": "^3.14.1",
    "prettier": "^1.17.1",
    "ts-node": "^8.1.1",
    "typescript": "^3.4.5"
  }
}
